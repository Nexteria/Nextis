import { Record } from 'immutable';

const StudentLevel = Record({
  id: null,
  name: '',
  description: '',
  owneId: null,
  created_at: null,
  updated_at: null,
});

export default StudentLevel;
